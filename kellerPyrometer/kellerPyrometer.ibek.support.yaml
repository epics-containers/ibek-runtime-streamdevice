# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: kellerPyrometer 0-1
# Source: /dls_sw/prod/R3.14.12.7/support/kellerPyrometer/0-1
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/kellerPyrometer/ release.

module: kellerPyrometer

entity_models:
  - name: kellerPyrometer
    description: |-
      Keller Pyrometer
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      PORT:
        type: str
        description: |-
          Device port
    databases:
      - file: kellerPyrometer.template
        args:
          .*:
