# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/4.8.0-beta.2/ibek.support.schema.json

# Imported from DLS production support module: axisCameraControl 2-4
# Source: /dls_sw/prod/R3.14.12.7/support/axisCameraControl/2-4
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/axisCameraControl/ release.

module: axisCameraControl

entity_models:
  - name: axisCameraControl
    description: |-
      Control template for an Axis web camera (Pan/Tilt/Zoom control)
    parameters:
      P:
        type: str
        description: |-
          PV prefix
      PORT:
        type: str
        description: |-
          Asyn http port
    databases:
      - file: axisCameraControl.template
        args:
          .*:
