# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: ametekLockIn 1-17special2
# Source: /dls_sw/prod/R3.14.12.7/support/ametekLockIn/1-17special2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/ametekLockIn/ release.

module: ametekLockIn

entity_models:
  - name: ametekLockIn
    description: |-
      Ametek (Signal Recovery) lock-in amplifier
    parameters:
      P:
        type: str
        description: |-
          PV name prefix
      Q:
        type: str
        description: |-
          PV name suffix
      PORT:
        type: str
        description: |-
          asyn port
    databases:
      - file: ametekLockIn.template
        args:
          .*:
