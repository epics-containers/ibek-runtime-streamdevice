# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: newstep 2-6
# Source: /dls_sw/prod/R3.14.12.7/support/newstep/2-6
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/newstep/ release.

module: newstep

entity_models:
  - name: NSC200
    description: |-
      Newport NSC200 single axis motion controller
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      M:
        type: str
        description: |-
          Device suffix
      CH:
        type: int
        description: |-
          Controller channel address
      PORT:
        type: str
        description: |-
          Asyn port name
      name:
        type: id
        description: |-
          Object and gui association name
    databases:
      - file: NSC200.template
        args:
          .*:
