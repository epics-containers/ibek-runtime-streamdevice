# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: chemyxFusion 1-0
# Source: /dls_sw/prod/R3.14.12.7/support/chemyxFusion/1-0
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/chemyxFusion/ release.

module: chemyxFusion

entity_models:
  - name: Fusion4000
    description: |-
      Chemyx Fusion 4000 syringe pump controller
    parameters:
      P:
        type: str
        description: |-
          PV name prefix
      R:
        type: str
        description: |-
          PV name suffix
      PORT:
        type: str
        description: |-
          asyn port
      name:
        type: id
        description: |-
          module name
    databases:
      - file: fusion4000.template
        args:
          .*:
