# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: FQCRBarcodeCamera 2-7
# Source: /dls_sw/prod/R3.14.12.7/support/FQCRBarcodeCamera/2-7
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/FQCRBarcodeCamera/ release.

module: FQCRBarcodeCamera

entity_models:
  - name: FQCRBarcodeCamera
    description: |-
      FQCR Barcode camera (Omron camera + PLC barcode read-out).
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      cam_port:
        type: str
        description: |-
          Asyn Port for camera
      plc_port:
        type: str
        description: |-
          Asyn Port for plc
    databases:
      - file: FQCRBarcodeCamera.template
        args:
          .*:
