# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: bronkhorstFlowBus 1-5
# Source: /dls_sw/prod/R3.14.12.7/support/bronkhorstFlowBus/1-5
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/bronkhorstFlowBus/ release.

module: bronkhorstFlowBus

entity_models:
  - name: coriFlow
    description: |-
      Controls a bronkhorst coriFlow controller
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      Q:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          Asyn port name
      NODE:
        type: str
        description: |-
          Node number, normally 80 (hex for 128, broadcast)
        default: "80"
      name:
        type: id
        description: |-
          Object and gui association name
    databases:
      - file: coriFlow.template
        args:
          .*:
