# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: specsXRC1000 0-1
# Source: /dls_sw/prod/R3.14.12.7/support/specsXRC1000/0-1
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/specsXRC1000/ release.

module: specsXRC1000

entity_models:
  - name: specsXRC1000
    description: |-
      SPECS XRC1000 X-ray source controller
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      PORT:
        type: str
        description: |-
          terminal server asyn serial port number
    databases:
      - file: specsXRC1000.template
        args:
          .*:
