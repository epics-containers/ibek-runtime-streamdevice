# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: BELEKTRONIG_BTC 2-0
# Source: /dls_sw/prod/R3.14.12.7/support/BELEKTRONIG_BTC/2-0
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/BELEKTRONIG_BTC/ release.

module: BELEKTRONIG_BTC

entity_models:
  - name: belektronig_btc
    description: |-
      Belektronig BTC M10 temperature controller
    parameters:
      P:
        type: str
        description: |-
          Device name
      R:
        type: str
        description: |-
          Heater name
      DESC:
        type: str
        description: |-
          Description
      PORT:
        type: str
        description: |-
          Device asyn port
    databases:
      - file: m10.template
        args:
          .*:
