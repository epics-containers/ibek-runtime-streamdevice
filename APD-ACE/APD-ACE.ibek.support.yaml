# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: APD-ACE 2-3
# Source: /dls_sw/prod/R3.14.12.7/support/APD-ACE/2-3
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/APD-ACE/ release.

module: APD-ACE

entity_models:
  - name: APDACE
    description: |-
      APD ACE avalanche photodiode bias/control unit
    parameters:
      P:
        type: str
        description: |-
          Device name
      Q:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          asyn port
      name:
        type: id
        description: |-
          Object and gui association name
    databases:
      - file: apd-ace.template
        args:
          .*:
