# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: opSens 1-1
# Source: /dls_sw/prod/R3.14.12.7/support/opSens/1-1
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/opSens/ release.

module: opSens

entity_models:
  - name: opSens
    description: |-
      Controls opSens Optical Temperature sensor
    parameters:
      device:
        type: str
        description: |-
          Device name
      tty:
        type: str
        description: |-
          Terminal name
    databases:
      - file: opSens.template
        args:
          .*:
