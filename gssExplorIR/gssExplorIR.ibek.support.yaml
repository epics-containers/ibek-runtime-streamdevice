# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: gssExplorIR 1-2
# Source: /dls_sw/prod/R3.14.12.7/support/gssExplorIR/1-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/gssExplorIR/ release.

module: gssExplorIR

entity_models:
  - name: explorir
    description: |-
      GSS ExplorIR-M-100 CO2 Sensor
    parameters:
      P:
        type: str
        description: |-
          Prefix for PV name
      PORT:
        type: str
        description: |-
          Bus/Port Address (eg. ASYN Port).
      SCAN:
        type: int
        description: |-
          SCAN rate for read parameters.
      name:
        type: id
        description: |-
          object and gui association name
    databases:
      - file: explorir.template
        args:
          .*:
