# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: EdwardsScroll 1-15
# Source: /dls_sw/prod/R3.14.12.7/support/EdwardsScroll/1-15
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/EdwardsScroll/ release.

module: EdwardsScroll

entity_models:
  - name: EdwardsScroll
    description: |-
      Template for Serial control and monitoring of Edwards Scroll pumps via serial.  Usually in collaboration with a PLC which has On/Off control.
    parameters:
      P:
        type: str
        description: |-
          Mandatory. Device prefix
      port:
        type: str
        description: |-
          Mandatory. Asyn port name
      name:
        type: id
        description: |-
          Optional.  Name for gui element
    databases:
      - file: EdwardsScroll.template
        args:
          .*:
