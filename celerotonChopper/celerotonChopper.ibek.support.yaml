# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: celerotonChopper 1-1
# Source: /dls_sw/prod/R3.14.12.7/support/celerotonChopper/1-1
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release,
# except for these scripted changes:
# - celerotonChopper.template: DRVH/DRVL dropped from one ai record (not ai
#   fields; EPICS 7 rejects the file with them).
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/celerotonChopper/ release.

module: celerotonChopper

entity_models:
  - name: celerotonChopper
    description: |-
      Celeroton chopper magnetic bearing motor controller
    parameters:
      P:
        type: str
        description: |-
          PV name prefix
      Q:
        type: str
        description: |-
          PV name suffix
      PORT:
        type: str
        description: |-
          asyn port
    databases:
      - file: celerotonChopper.template
        args:
          .*:
