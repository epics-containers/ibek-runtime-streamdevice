# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: Compax3 0-2
# Source: /dls_sw/prod/R3.14.12.7/support/Compax3/0-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/Compax3/ release.

module: Compax3

entity_models:
  - name: Compax3
    description: |-
      Parker Compax3 motor controller (ASCII serial interface)
    parameters:
      prefix:
        type: str
        description: |-
          PV name prefix
      suff1:
        type: str
        description: |-
          First PV name suffix segment
      suff2:
        type: str
        description: |-
          Second PV name suffix segment
      port1:
        type: str
        description: |-
          First asyn port name segment
      port2:
        type: str
        description: |-
          Second asyn port name segment
    databases:
      - file: Compax3.template
        args:
          .*:
