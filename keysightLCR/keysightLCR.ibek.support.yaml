# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: keysightLCR 0-2
# Source: /dls_sw/prod/R3.14.12.7/support/keysightLCR/0-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/keysightLCR/ release.

module: keysightLCR

entity_models:
  - name: keysightLCR
    description: |-
      Keysight LCR meter
    parameters:
      P:
        type: str
        description: |-
          Prefix for PV name
      Q:
        type: str
        description: |-
          Suffix for PV name
      PORT:
        type: str
        description: |-
          Bus/Port Address (eg. ASYN Port)
      ADDR:
        type: int
        description: |-
          Address on the bus (optional)
      SCAN:
        type: int
        description: |-
          SCAN rate for read parameters
    databases:
      - file: keysightLCR.template
        args:
          .*:
