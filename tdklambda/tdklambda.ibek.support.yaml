# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/4.8.0-beta.2/ibek.support.schema.json

# Imported from DLS production support module: tdklambda 1-6
# Source: /dls_sw/prod/R3.14.12.7/support/tdklambda/1-6
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release
# (1-6's db/ is byte-identical to 1-5's).
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/tdklambda/ release.

module: tdklambda

entity_models:
  - name: Gen3300w
    description: |-
      TDK Lambda 3300W power supply
    parameters:
      P:
        type: str
        description: |-
          EPICS name prefix
      PORT:
        type: str
        description: |-
          asyn port
      CMDSET:
        type: str
        description: |-
          Command set: use 'R' for RS232(default) or 'S' for SCPI(Ethernet)
        default: R
    databases:
      - file: gen3300w.template
        args:
          .*:
