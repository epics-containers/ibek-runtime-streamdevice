# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

module: polypico

entity_models:
  - name: polypico
    description: |-
      Polypico piezo dispenser StreamDevice control
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      PORT:
        type: str
        description: |-
          Asyn port name for stream device
    databases:
      - file: polypico.template
        args:
          .*:
