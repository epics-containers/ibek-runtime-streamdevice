# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: telna 1-3
# Source: /dls_sw/prod/R3.14.12.7/support/telna/1-3
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/telna/ release.

module: telna

entity_models:
  - name: telna
    description: |-
      Reads out the 8 channel Telna temperature logger
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      PORT:
        type: str
        description: |-
          Asyn Port name
    databases:
      - file: telna.template
        args:
          .*:
