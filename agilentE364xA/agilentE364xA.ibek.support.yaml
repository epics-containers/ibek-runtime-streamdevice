# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: agilentE364xA 1-2-1
# Source: /dls_sw/prod/R3.14.12.7/support/agilentE364xA/1-2-1
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/agilentE364xA/ release.

module: agilentE364xA

entity_models:
  - name: E364xA
    description: |-
      Agilent E364xA programmable DC power supply
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      R:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          Asyn port name
      PREC:
        type: int
        description: |-
          decimal precision to use. Default=3
        default: 3
    databases:
      - file: E364xA.template
        args:
          .*:

  - name: caputcallback
    description: |-
      Imported from DLS module agilentE364xA (caputcallback.template)
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      M:
        type: str
        description: |-
          M
      TWSIZE:
        type: float
        description: |-
          TWSIZE
        default: 0.1
      PREC:
        type: int
        description: |-
          Precision (number of decimal places)
        default: 3
      EGU:
        type: str
        description: |-
          Engineering units
        default: "V"
      CPCB_RBV:
        type: str
        description: |-
          CPCB_RBV
      DEADBAND:
        type: float
        description: |-
          DEADBAND
        default: 0.01
      CPCB_SET:
        type: str
        description: |-
          CPCB_SET
    databases:
      - file: caputcallback.template
        args:
          .*:
