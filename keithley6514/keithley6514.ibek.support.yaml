# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: keithley6514 0-4
# Source: /dls_sw/prod/R3.14.12.7/support/keithley6514/0-4
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/keithley6514/ release.

module: keithley6514

entity_models:
  - name: keithley6514
    description: |-
      Keithley 6514 System Electrometer
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      R:
        type: str
        description: |-
          Device Suffix
      PORT:
        type: str
        description: |-
          Asyn Port name
      A:
        type: str
        description: |-
          Timeout
    databases:
      - file: keithley6514.template
        args:
          .*:
