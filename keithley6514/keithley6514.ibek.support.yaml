# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

module: keithley6514

entity_models:
  - name: keithley6514
    description: |-
      Keithley 6514 System Electrometer
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      R:
        type: str
        description: |-
          Device Suffix
      PORT:
        type: str
        description: |-
          Asyn Port name
      A:
        type: str
        description: |-
          Timeout
    databases:
      - file: keithley6514.template
        args:
          .*:
