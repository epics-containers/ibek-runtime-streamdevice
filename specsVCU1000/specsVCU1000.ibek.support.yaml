# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: specsVCU1000 1-9-2
# Source: /dls_sw/prod/R3.14.12.7/support/specsVCU1000/1-9-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/specsVCU1000/ release.

module: specsVCU1000

entity_models:
  - name: specsVCU1000
    description: |-
      SPECS VCU1000 controller
    parameters:
      name:
        type: id
        description: |-
          Object name
      P:
        type: str
        description: |-
          Device PV prefix 1
      Q:
        type: str
        description: |-
          Device PV prefix 2 (can be "")
      PORT:
        type: str
        description: |-
          Asyn UDP port name which connects to the VCU port (xxx.xxx.xxx.xxx:2005 UDP)
    databases:
      - file: specsVCU1000.template
        args:
          .*:
