# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: WS300scale 1-5
# Source: /dls_sw/prod/R3.14.12.7/support/WS300scale/1-5
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/WS300scale/ release.

module: WS300scale

entity_models:
  - name: WS300
    description: |-
      Controls a WS-300 Brecknell scale
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      S:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          asyn port
      name:
        type: id
        description: |-
          object and gui association name
    databases:
      - file: WS300.template
        args:
          .*:
