#! Generated by VisualDCT v2.7.1-SNAPSHOT
#! DBDSTART
#! DBD("../../dbd/ttiMX.dbd")
#! DBDEND


# Macros:
# % macro, P, The prefix name
# % macro, PORT, The asyn port
# %gui, $(name), edm, ttiMX_4channel.edl, P=$(P)
#
# The error register
# %archiver 1 Monitor VAL
record(mbbi, "$(P):ERROR") {
  field(DESC, "Error status")
  field(DTYP, "stream")
  field(INP, "@ttiMX.proto getError $(PORT)")
  field(SCAN, "1 second")
  field(ZRST, "No error")
  field(ONST, "Numeric error")
  field(TWST, "Recall error")
  field(UNSV, "MAJOR")
  field(ZRSV, "NO_ALARM")
  field(ONSV, "MINOR")
  field(TWSV, "MAJOR")
  field(PINI, "YES")
  field(ZRVL, "0")
  field(ONVL, "100")
  field(TWVL, "102")
  field(THVL, "103")
  field(FRVL, "200")
  field(THST, "Command Invalid")
  field(FRST, "Access denied")
  field(THSV, "MAJOR")
  field(FRSV, "MAJOR")
  field(SDIS, "$(P):POLLING")
}

record(stringin, "$(P):IDENT") {
  field(DESC, "Identity")
  field(DTYP, "stream")
  field(INP, "@ttiMX.proto getIDN $(PORT)")
  field(SCAN, "10 second")
  field(SDIS, "$(P):POLLING")
}

# Reset the instrument
# Just write to .PROC to invoke
# ZNAM, ONAM have no effect.
record(bo, "$(P):RESET") {
  field(DESC, "Reset supply")
  field(DTYP, "stream")
  field(OUT, "@ttiMX.proto reset $(PORT)")
  field(ZNAM, "Off")
  field(ONAM, "On")
}

# Disables ttiMX_channle.template comms polling
record(bo, "$(P):POLLING") {
  field(DESC, "Disables comms cmd polling")
  field(PINI, "YES")
  field(ZNAM, "Enabled")
  field(ONAM, "Disabled")
  field(OSV, "MAJOR")
  field(ZSV, "NO_ALARM")
}

#! Further lines contain data used by VisualDCT
#! View(0,91,1.0)
#! Record("$(P):ERROR",40,118,0,0,"$(P):ERROR")
#! Field("$(P):ERROR.SDIS",16777215,1,"$(P):ERROR.SDIS")
#! Link("$(P):ERROR.SDIS","$(P):POLLING.VAL")
#! Record("$(P):IDENT",340,354,0,0,"$(P):IDENT")
#! Field("$(P):IDENT.SDIS",16777215,0,"$(P):IDENT.SDIS")
#! Link("$(P):IDENT.SDIS","$(P):POLLING.VAL")
#! Record("$(P):RESET",540,114,0,0,"$(P):RESET")
#! Record("$(P):POLLING",320,80,0,1,"$(P):POLLING")
#! Field("$(P):POLLING.VAL",16777215,0,"$(P):POLLING.VAL")
