# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: CanberraG64 1-7
# Source: /dls_sw/prod/R3.14.12.7/support/CanberraG64/1-7
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/CanberraG64/ release.

module: CanberraG64

entity_models:
  - name: CanberraG64
    description: |-
      Imported from DLS module CanberraG64 (CanberraG64.template)
    parameters:
      device:
        type: str
        description: |-
          Device name
      port:
        type: str
        description: |-
          Asyn port name
      adel:
        type: str
        description: |-
          adel
    databases:
      - file: CanberraG64.template
        args:
          .*:
