# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: debenOF 2-0
# Source: /dls_sw/prod/R3.14.12.7/support/debenOF/2-0
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/debenOF/ release.

module: debenOF

entity_models:
  - name: debenOF
    description: |-
      Deben Open Frame tensile/torsion rig controller
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      R:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          Asyn port name for comms
    databases:
      - file: debenOF.template
        args:
          .*:
