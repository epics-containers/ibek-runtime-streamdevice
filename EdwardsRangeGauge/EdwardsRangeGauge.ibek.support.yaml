# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: EdwardsRangeGauge 23-4
# Source: /dls_sw/prod/R3.14.12.7/support/EdwardsRangeGauge/23-4
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/EdwardsRangeGauge/ release.

module: EdwardsRangeGauge

entity_models:
  - name: EdwardsRangeGauge
    description: |-
      Template for Serial control and monitoring of Edwards Pressure pumps via serial.  Usually in collaboration with a PLC which has On/Off control.
    parameters:
      P:
        type: str
        description: |-
          Mandatory. Device prefix
      port:
        type: str
        description: |-
          Mandatory. Asyn port name
      name:
        type: id
        description: |-
          Optional.  Name for gui element
    databases:
      - file: EdwardsGauge.template
        args:
          .*:
