# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: cognexDataMan100 1-2
# Source: /dls_sw/prod/R3.14.12.7/support/cognexDataMan100/1-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/cognexDataMan100/ release.

module: cognexDataMan100

entity_models:
  - name: dataman100
    description: |-
      Cognex DataMan 100 barcode reader
    parameters:
      P:
        type: str
        description: |-
          prefix
      R:
        type: str
        description: |-
          suffix
      PORT:
        type: str
        description: |-
          asyn port name
      name:
        type: id
        description: |-
          gui element name
    databases:
      - file: dataman100.template
        args:
          .*:
