# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: mks647c 0-1-4
# Source: /dls_sw/prod/R3.14.12.7/support/mks647c/0-1-4
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/mks647c/ release.

module: mks647c

entity_models:
  - name: mks647cChan
    description: |-
      MKS 647C mass flow controller channel
    parameters:
      P:
        type: str
        description: |-
          Prefix for PV name
      Q:
        type: str
        description: |-
          Suffix for PV name
      PORT:
        type: str
        description: |-
          Bus/Port Address (eg. ASYN Port).
      CHAN:
        type: int
        description: |-
          Channel number
    databases:
      - file: mks647cChan.template
        args:
          .*:
  - name: mks647c8ChanCtrl
    description: |-
      MKS 647C 8 channel mass flow controller
    parameters:
      P:
        type: str
        description: |-
          Prefix for PV name
      Q:
        type: str
        description: |-
          Suffix for PV name
      PORT:
        type: str
        description: |-
          Bus/Port Address (eg. ASYN Port).
    databases:
      - file: mks647c8ChanCtrl.template
        args:
          .*:
  - name: mks647c4ChanCtrl
    description: |-
      MKS 647C 4 channel mass flow controller
    parameters:
      P:
        type: str
        description: |-
          Prefix for PV name
      Q:
        type: str
        description: |-
          Suffix for PV name
      PORT:
        type: str
        description: |-
          Bus/Port Address (eg. ASYN Port).
    databases:
      - file: mks647c4ChanCtrl.template
        args:
          .*:
