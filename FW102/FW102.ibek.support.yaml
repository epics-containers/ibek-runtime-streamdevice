# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: FW102 1.0
# Source: /dls_sw/prod/R3.14.12.7/support/FW102/1.0
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/FW102/ release.

module: FW102

entity_models:
  - name: _fw102Template
    description: |-
      Thorlabs FW102 motorised filter wheel
    parameters:
      P:
        type: str
        description: |-
          PV name prefix
      R:
        type: str
        description: |-
          PV name suffix
      PORT:
        type: str
        description: |-
          asyn port name
      name:
        type: id
        description: |-
          module name
    databases:
      - file: fw102.template
        args:
          .*:
