# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: specsEBE 0-7
# Source: /dls_sw/prod/R3.14.12.7/support/specsEBE/0-7
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/specsEBE/ release.

module: specsEBE

entity_models:
  - name: EBE4
    description: |-
      Specs EBE-4 template file.
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      PORT:
        type: str
        description: |-
          Asyn Port name
      SCAN:
        type: int
        description: |-
          Poll rate
    databases:
      - file: ebe4.db
        args:
          .*:
