# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: huberChiller 1-3
# Source: /dls_sw/prod/R3.14.12.7/support/huberChiller/1-3
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/huberChiller/ release.

module: huberChiller

entity_models:
  - name: huber
    description: |-
      Huber chiller control using streams
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      R:
        type: str
        description: |-
          Device Suffix
      PORT:
        type: str
        description: |-
          Asyn Port name
      name:
        type: id
        description: |-
          gui element name
    databases:
      - file: huberChiller.template
        args:
          .*:
