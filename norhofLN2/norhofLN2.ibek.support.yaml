# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: norhofLN2 2-3
# Source: /dls_sw/prod/R3.14.12.7/support/norhofLN2/2-3
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/norhofLN2/ release.

module: norhofLN2

entity_models:
  - name: norhofLN2
    description: |-
      Controls a Norhof LN2 Pump
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      PORT:
        type: str
        description: |-
          Asyn Port
      name:
        type: id
        description: |-
          Object and gui association name
    databases:
      - file: norhofLN2.template
        args:
          .*:
