# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: leyboldCenterOne 1-1
# Source: /dls_sw/prod/R3.14.12.7/support/leyboldCenterOne/1-1
# The built db/ output has $(X,undefined) baked into record names for some
# templates; those are shipped from the module's App/Db msi source instead.
# Everything else is a pristine copy of that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/leyboldCenterOne/ release.

module: leyboldCenterOne

entity_models:
  - name: centerOne
    description: |-
      Leybold CenterOne vacuum gauge controller - reads pressure with a
      validity watchdog over StreamDevice.
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      STREAMS_PORT:
        type: str
        description: |-
          Stream/serial port (Asyn Port)
    databases:
      - file: centerOne.template
        args:
          .*:
