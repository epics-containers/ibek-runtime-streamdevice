# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: agilent33220A 1-7
# Source: /dls_sw/prod/R3.14.12.7/support/agilent33220A/1-7
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/agilent33220A/ release.

module: agilent33220A

entity_models:
  - name: agilent33220A
    description: |-
      Agilent 33220A function/arbitrary waveform generator
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      Q:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          Asyn port name
      name:
        type: id
        description: |-
          Name to export to gda as
    databases:
      - file: agilent33220A.template
        args:
          .*:
