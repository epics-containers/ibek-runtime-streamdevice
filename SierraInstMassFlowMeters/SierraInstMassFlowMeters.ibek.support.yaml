# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: SierraInstMassFlowMeters 1-0
# Source: /dls_sw/prod/R3.14.12.7/support/SierraInstMassFlowMeters/1-0
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/SierraInstMassFlowMeters/ release.

module: SierraInstMassFlowMeters

entity_models:
  - name: SierraInstMassFlowMeters
    description: |-
      Sierra Instruments mass flow meter / controller support.
    parameters:
      P:
        type: str
        description: |-
          Device PV prefix 1
      Q:
        type: str
        description: |-
          Device PV prefix 2
      PORT:
        type: str
        description: |-
          Asyn port name for connection with device
      name:
        type: id
        description: |-
          Object and gui association name
    databases:
      - file: SierraInstMassFlowMeters.template
        args:
          .*:
