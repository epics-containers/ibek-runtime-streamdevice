# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: agilent53220 1-3-4
# Source: /dls_sw/prod/R3.14.12.7/support/agilent53220/1-3-4
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/agilent53220/ release.

module: agilent53220

entity_models:
  - name: agilent53220
    description: |-
      Agilent 53220 frequency analyser
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      Q:
        type: str
        description: |-
          Device suffix
      PORT:
        type: str
        description: |-
          Asyn port name
      name:
        type: id
        description: |-
          Name to export to gda as
    databases:
      - file: agilent53220.template
        args:
          .*:
