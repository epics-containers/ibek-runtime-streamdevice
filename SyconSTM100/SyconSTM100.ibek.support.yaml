# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: SyconSTM100 0-2
# Source: /dls_sw/prod/R3.14.12.7/support/SyconSTM100/0-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/SyconSTM100/ release.

module: SyconSTM100

entity_models:
  - name: SyconSTM100
    description: |-
      Sycon STM-100 thin film deposition monitor
    parameters:
      P:
        type: str
        description: |-
          Device prefix
      PORT:
        type: str
        description: |-
          asyn serial port number
    databases:
      - file: SyconSTM100.template
        args:
          .*:
