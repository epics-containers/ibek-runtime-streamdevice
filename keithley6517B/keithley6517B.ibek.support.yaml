# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: keithley6517B 1-2
# Source: /dls_sw/prod/R3.14.12.7/support/keithley6517B/1-2
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/keithley6517B/ release.

module: keithley6517B

entity_models:
  - name: keithley6517B
    description: |-
      Keithley 6517B Electrometer
    parameters:
      P:
        type: str
        description: |-
          Device Prefix
      R:
        type: str
        description: |-
          Device Suffix
      PORT:
        type: str
        description: |-
          Asyn Port name
      A:
        type: str
        description: |-
          Timeout
    databases:
      - file: keithley6517B.template
        args:
          .*:
