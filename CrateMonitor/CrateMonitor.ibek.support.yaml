# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/4.8.0-beta.2/ibek.support.schema.json

# Imported from DLS production support module: CrateMonitor 2-4
# Source: /dls_sw/prod/R3.14.12.7/support/CrateMonitor/2-4
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/CrateMonitor/ release.

module: CrateMonitor

entity_models:
  - name: CrateMonitor
    description: |-
      VME Crate Monitor (fan speeds, supply voltages, temperatures and status).
    parameters:
      device:
        type: str
        description: |-
          Asyn IP Port, also used for device prefix
    databases:
      - file: CrateMonitor.template
        args:
          .*:
