# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: thermocube 1-0
# Source: /dls_sw/prod/R3.14.12.7/support/thermocube/1-0
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/thermocube/ release.

module: thermocube

entity_models:
  - name: thermoCube
    description: |-
      ThermoCube chiller
    parameters:
      P:
        type: str
        description: |-
          PV name prefix
      R:
        type: str
        description: |-
          PV name suffix
      PORT:
        type: str
        description: |-
          asyn port
      name:
        type: id
        description: |-
          module name
    databases:
      - file: thermocube.template
        args:
          .*:
