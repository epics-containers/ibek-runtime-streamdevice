# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: cyberstar 2-3-1
# Source: /dls_sw/prod/R3.14.12.7/support/cyberstar/2-3-1
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/cyberstar/ release.

module: cyberstar

entity_models:
  - name: CBY_2206
    description: |-
      Cyberstar CBY-2206 scintillation detector amplifier/discriminator
    parameters:
      P:
        type: str
        description: |-
          device name
      PORT:
        type: str
        description: |-
          asyn port
      ADDR:
        type: int
        description: |-
          device address
      name:
        type: id
        description: |-
          GUI name
    databases:
      - file: CBY-2206.template
        args:
          .*:
