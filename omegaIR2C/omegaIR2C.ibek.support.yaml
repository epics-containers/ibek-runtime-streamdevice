# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: omegaIR2C 1-5
# Source: /dls_sw/prod/R3.14.12.7/support/omegaIR2C/1-5
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/omegaIR2C/ release.

module: omegaIR2C

entity_models:
  - name: omegaIR2C
    description: |-
      Controls an Omega iR2C Pyrometer
    parameters:
      name:
        type: id
        description: |-
          Object name
      P:
        type: str
        description: |-
          Device prefix
      ADDR:
        type: int
        description: |-
          Device address (e.g. 01)
      PORT:
        type: str
        description: |-
          Asyn port name
    databases:
      - file: omegaIR2C.template
        args:
          .*:
