# yaml-language-server: $schema=https://github.com/epics-containers/ibek/releases/download/3.0.1/ibek.support.schema.json

# Imported from DLS production support module: daylightQCL 0-3
# Source: /dls_sw/prod/R3.14.12.7/support/daylightQCL/0-3
# Pristine copy of the StreamDevice protocol + EPICS db/template from that release.
# To check for updates: diff this pattern's files against that path, or against the
# newest /dls_sw/prod/*/support/daylightQCL/ release.

module: daylightQCL

entity_models:
  - name: daylightQCL
    description: |-
      Daylight Solutions quantum cascade laser (QCL) controller
    parameters:
      P:
        type: str
        description: |-
          PV name prefix
      Q:
        type: str
        description: |-
          PV name suffix
      PORT:
        type: str
        description: |-
          asyn port
    databases:
      - file: daylightQCL.template
        args:
          .*:
